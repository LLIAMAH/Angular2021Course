export enum AlertType {
  Primary,
  Secondary,
  Success,
  Danger,
  Warning,
  Info,
  Light,
  Dark,
  End
}

export enum ServerStatus {
  Offline,
  Online
}

export enum EnumFeatureSelected {
  Recipes,
  ShoppingList
}
